//this class is the one with the main() that starts and handles arguments
public class MainClient {
    public ClientFactory factory=new ClientFactory();
    public Client lrr=factory.getClient("lrr");
}
